import React from "react";

export interface URLSubdomainActionProps {
    url: string;
    onComplete: (matches: string[]) => void;
}

export default function URLSubdomainAction(props: URLSubdomainActionProps) {

    return (
        <div></div>
    )
}